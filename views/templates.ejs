<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Template Builder | Invoice Agent</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.13/codemirror.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.13/theme/dracula.min.css">
    <style>
        body { overflow: hidden; }
        .builder-wrap {
            display: flex;
            flex-direction: column;
            height: calc(100vh - 56px);
            padding: 16px 20px;
            gap: 14px;
            box-sizing: border-box;
        }
        .builder-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-shrink: 0;
        }
        .builder-header h1 { font-size: 1.4rem; margin: 0; }
        .builder-header p { color: #64748b; font-size: 0.85rem; margin: 2px 0 0; }
        .model-bar {
            background: white;
            border-radius: 10px;
            padding: 12px 16px;
            box-shadow: 0 1px 4px rgba(0,0,0,0.08);
            display: flex;
            gap: 10px;
            align-items: flex-end;
            flex-wrap: wrap;
            flex-shrink: 0;
        }
        .model-bar .form-group-inline { flex: 1; min-width: 140px; }
        .builder-layout {
            display: grid;
            grid-template-columns: 380px 1fr;
            gap: 14px;
            flex: 1;
            min-height: 0;
        }
        .panel {
            background: #fff;
            border-radius: 10px;
            padding: 18px;
            box-shadow: 0 1px 4px rgba(0,0,0,0.08);
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }
        .panel-title {
            font-size: 0.75rem;
            font-weight: 800;
            letter-spacing: 1px;
            text-transform: uppercase;
            color: #94a3b8;
            margin: 0 0 10px;
        }
        .step-label {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 0.9rem;
            font-weight: 700;
            color: #1e293b;
            margin-bottom: 8px;
        }
        .step-num {
            width: 22px; height: 22px;
            background: #6366f1;
            color: white;
            border-radius: 50%;
            font-size: 0.75rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 800;
            flex-shrink: 0;
        }
        .upload-zone {
            border: 2px dashed #cbd5e1;
            border-radius: 8px;
            padding: 18px;
            text-align: center;
            cursor: pointer;
            transition: all 0.2s;
            background: #f8fafc;
            position: relative;
            margin-bottom: 10px;
        }
        .upload-zone:hover { border-color: #6366f1; background: #f0f1ff; }
        .upload-zone input[type=file] {
            position: absolute; inset: 0; opacity: 0; cursor: pointer; width: 100%; height: 100%;
        }
        .upload-zone .upload-icon { font-size: 1.8rem; margin-bottom: 6px; }
        .upload-zone p { font-size: 0.8rem; color: #64748b; margin: 0; }
        .upload-zone .file-chosen { font-size: 0.85rem; font-weight: 600; color: #6366f1; margin-top: 6px; display: none; }
        .divider {
            border: none;
            border-top: 1px solid #f1f5f9;
            margin: 14px 0;
        }
        .template-name-row { display: flex; gap: 8px; }
        .template-name-row input {
            flex: 1; padding: 9px 12px; border: 1px solid #cbd5e1;
            border-radius: 6px; font-size: 0.9rem;
        }
        .gen-log {
            flex: 1;
            overflow-y: auto;
            font-family: monospace;
            font-size: 0.78rem;
            min-height: 0;
            padding: 8px;
            background: #f8fafc;
            border-radius: 6px;
            border: 1px solid #f1f5f9;
            margin-top: 10px;
        }
        .log-info  { color: #475569; }
        .log-success { color: #16a34a; font-weight: 600; }
        .log-error { color: #dc2626; font-weight: 600; }
        .tabs-bar { display: flex; gap: 4px; margin-bottom: 10px; flex-shrink: 0; }
        .tab-btn {
            padding: 7px 16px;
            border-radius: 6px;
            border: none;
            background: #f1f5f9;
            color: #64748b;
            font-size: 0.85rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.15s;
        }
        .tab-btn.active { background: #6366f1; color: white; }
        .refresh-btn {
            margin-left: auto;
            padding: 7px 14px;
            border: 1px solid #cbd5e1;
            border-radius: 6px;
            background: white;
            color: #475569;
            font-size: 0.8rem;
            cursor: pointer;
            transition: all 0.15s;
        }
        .refresh-btn:hover { border-color: #6366f1; color: #6366f1; }
        .editor-container {
            flex: 1;
            border: 1px solid #e2e8f0;
            border-radius: 6px;
            overflow: hidden;
            min-height: 0;
        }
        .CodeMirror { height: 100% !important; font-size: 0.82rem; }
        iframe { width: 100%; height: 100%; border: none; background: #f8fafc; display: block; }
        .empty-state {
            width: 100%; height: 100%;
            display: flex; flex-direction: column;
            align-items: center; justify-content: center;
            color: #94a3b8; gap: 8px; text-align: center; padding: 20px;
        }
        .empty-state .es-icon { font-size: 3rem; }
        .empty-state p { font-size: 0.85rem; margin: 0; }
        .btn-indigo {
            background: #6366f1; color: white; border: none;
            padding: 10px 18px; border-radius: 8px; font-size: 0.9rem;
            font-weight: 700; cursor: pointer; width: 100%; transition: background 0.2s;
        }
        .btn-indigo:hover { background: #4f46e5; }
        .btn-indigo:disabled { background: #a5b4fc; cursor: not-allowed; }
        .btn-green {
            background: #16a34a; color: white; border: none;
            padding: 9px 14px; border-radius: 6px; font-size: 0.85rem;
            font-weight: 700; cursor: pointer; white-space: nowrap; transition: background 0.2s;
        }
        .btn-green:hover { background: #15803d; }
        .chips-row { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 6px; }
        .chip {
            padding: 3px 10px; border-radius: 20px; font-size: 0.75rem;
            cursor: pointer; border: 1px solid #e2e8f0; background: #f8fafc;
            color: #475569; transition: all 0.15s;
        }
        .chip:hover { border-color: #6366f1; color: #6366f1; }
        .chip.vision { border-color: #bbf7d0; background: #f0fdf4; color: #15803d; }
        .chip.vision:hover { border-color: #16a34a; }
    </style>
</head>
<body>
    <nav class="navbar">
        <a href="/" class="nav-brand">Invoice Agent</a>
        <div class="nav-links">
            <a href="/">Upload</a>
            <a href="/builder" class="active">Template Builder</a>
            <a href="/settings">Settings</a>
        </div>
    </nav>

    <div class="builder-wrap">
        <!-- Header -->
        <div class="builder-header">
            <div>
                <h1>Template Builder</h1>
                <p>Upload an invoice image → AI reconstructs a pixel-perfect HTML/EJS template → save and reuse.</p>
            </div>
        </div>

        <!-- AI Model Bar -->
        <div class="model-bar">
            <div class="form-group-inline">
                <label for="providerSelect">AI Provider</label>
                <select id="providerSelect"><option value="">Loading...</option></select>
            </div>
            <div class="form-group-inline">
                <label for="modelInput">Vision Model</label>
                <input type="text" id="modelInput" placeholder="e.g. gpt-4o">
            </div>
            <div class="test-section">
                <button type="button" class="btn-test" id="testBtn">Test</button>
                <div class="status-light" id="statusLight" title="Click Test to check connection">
                    <span class="light gray"></span>
                    <span class="status-text" id="statusText">Not tested</span>
                </div>
            </div>
        </div>
        <!-- Model chips (vision-only) -->
        <div class="chips-row" id="modelChips" style="margin-top: -6px; padding: 0 4px;"></div>

        <!-- Main Layout -->
        <div class="builder-layout">
            <!-- Left Panel -->
            <div class="panel">
                <p class="panel-title">Configuration</p>

                <!-- Step 1 -->
                <div class="step-label"><span class="step-num">1</span> Upload Reference Invoice</div>
                <p style="font-size:0.82rem;color:#64748b;margin:0 0 10px;">Upload a sample scanned invoice or PDF for AI to reconstruct.</p>
                <div class="upload-zone" id="uploadZone">
                    <input type="file" id="referenceImage" accept="image/*,.pdf">
                    <div class="upload-icon">🖼️</div>
                    <p>Click or drag & drop image / PDF</p>
                    <div class="file-chosen" id="fileChosen"></div>
                </div>

                <button class="btn-indigo" id="genBtn" disabled>
                    <span id="genBtnText">&#10024; Generate Template</span>
                </button>

                <hr class="divider">

                <!-- Step 2 -->
                <div class="step-label"><span class="step-num">2</span> Save Template</div>
                <p style="font-size:0.82rem;color:#64748b;margin:0 0 10px;">Give it a name to save as an <code>.ejs</code> template file.</p>
                <div class="template-name-row">
                    <input type="text" id="templateName" placeholder="e.g. standard_gst">
                    <button class="btn-green" id="saveBtn">&#128190; Save</button>
                </div>

                <!-- Log -->
                <div class="gen-log" id="genLog">
                    <div class="log-info">Ready. Select a vision model and upload an invoice to begin.</div>
                </div>
            </div>

            <!-- Right Panel: Preview / Code -->
            <div class="panel">
                <div class="tabs-bar">
                    <button class="tab-btn active" data-tab="preview">&#128207; Preview (PDF)</button>
                    <button class="tab-btn" data-tab="code">&#128196; HTML Source</button>
                    <button class="refresh-btn" id="refreshPreviewBtn">&#8635; Refresh</button>
                </div>

                <div id="previewTab" class="editor-container">
                    <div class="empty-state" id="emptyState">
                        <span class="es-icon">&#128444;&#65039;</span>
                        <p>Generate a template to see the preview here.</p>
                    </div>
                    <iframe id="previewFrame" style="display:none;"></iframe>
                </div>

                <div id="codeTab" class="editor-container" style="display: none;">
                    <textarea id="htmlEditor"></textarea>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.13/codemirror.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.13/mode/xml/xml.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.13/mode/javascript/javascript.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.13/mode/css/css.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.13/mode/htmlmixed/htmlmixed.min.js"></script>
    <script src="/js/pages/templates.js"></script>
</body>
</html>
